<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.TFM" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue_500</item>
        <item name="colorPrimaryVariant">@color/blue_700</item>
        <item name="colorOnPrimary">@color/black</item> <!-- Texto negro sobre color primario claro -->
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/green_500</item>
        <item name="colorSecondaryVariant">@color/green_700</item>
        <item name="colorOnSecondary">@color/black</item> <!-- Texto negro sobre color secundario claro -->
        <!-- Accent color. -->
        <item name="colorAccent">@color/orange_500</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="backgroundColor">@color/backgroundColorNight</item>
        <item name="cardBackgroundColor">@color/cardColorNight</item>
        <!-- Text colors. -->
        <item name="android:textColorPrimary">@color/white</item> <!-- Texto claro en fondo oscuro -->
        <item name="android:textColorSecondary">@color/gray_100</item> <!-- Texto oscuro en fondo claro -->
        <!-- Customize your theme here. -->
        <item name="iconTint">@color/white</item>

    </style>

    <style name="Theme.TFM.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.TFM.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>